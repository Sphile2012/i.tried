/**
 * Rate Limiter Middleware (Legacy)
 *
 * This file previously contained Express.js rate limiting middleware.
 * Rate limiting is now handled by NestJS ThrottlerModule (@nestjs/throttler).
 *
 * This file is kept as a stub to avoid breaking imports.
 * The NestJS application uses ThrottlerModule configured in app.module.ts.
 */

export const rateLimiter = null;
export const paymentRateLimiter = null;
export const authRateLimiter = null;
